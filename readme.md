# Indie Lee website

## How to run the code
1. $ ``` npm install && bower install && composer install ```
2. $ ``` grunt sass ```
3. $ ``` grunt watch ```

## Genetate docs
$ ``` php vendor/bin/phpdoc ``